server:
    port: 9002

mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.example.soacontent.model

management:
    endpoints:
        web:
            exposure:
                include: refresh